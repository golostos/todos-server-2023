{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "../src/*"
      ],
    },
  },
  "include": [
    "./**/*.ts", // Include all TypeScript files in '__tests__' directory
  ]
}