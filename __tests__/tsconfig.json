{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true, // Prevent files from being emitted
    "baseUrl": ".", /* Base directory to resolve non-absolute module names. */
    "paths": {
      "@/*": [
        "../src/*"
      ]
    },
  },
  "include": [
    "./**/*.ts" // Include all TypeScript files in '__tests__' directory
  ]
}